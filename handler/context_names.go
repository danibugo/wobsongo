package handler

const (
	contextRequestID          = "reqid"
	contextRequestToken       = "jwt"
	contextRequestCookieToken = "auth"
	contextRequestUser        = "user"
)

const (
	headerRequestID = "X-Request-ID"
)
